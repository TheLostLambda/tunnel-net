version: "3.8"

services:
  tunnel-net:
    build: .
    image: thelostlambda/tunnel-net:latest
    network_mode: "host"
    environment:
      link: enp34s0
      wanlink: wlo1
    privileged: true
    cap_add:
      - NET_ADMIN
